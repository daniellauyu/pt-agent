.DS_Store
.codegraph/
.claude/

# 本地参考仓库，不属于本项目源码。
reference-projects/

# 本地密钥：插件的预设配置和守护进程的 .env 都含下载器密码与站点 API Key。
# .env.example 是不含密钥的模板，那个要提交。
pt-agent-extension/private-config.js
pt-agent-daemon/.env
pt-agent-daemon/*.env
!pt-agent-daemon/.env.example

# Node 依赖（守护进程零依赖，这里只挡 npm link 产生的目录）。
pt-agent-daemon/node_modules/
pt-agent-daemon/package-lock.json

# 编辑器与临时文件。
.idea/
.vscode/
*.swp
*.tmp
